export function DashboardPage() {
  return (
    <main className="container stack">
      <div className="card stack">
        <h1>Dashboard</h1>
        <p>This is the logged-in landing page where you can link out to the course builder, semester planner, and saved schedules.</p>
      </div>
    </main>
  );
}
